/**
 * Login 页占位 — 批 1a step 10 实现 react-hook-form + zod + useLogin mutation。
 */
export function Login() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-surface-canvas px-xl text-text-primary">
      <div className="w-full max-w-sm rounded-xl bg-surface-card p-2xl shadow-card">
        <p className="text-label-caps text-text-muted uppercase">Auth</p>
        <h1 className="mt-md font-display text-h2">登录</h1>
        <p className="mt-sm font-body text-body-sm text-text-secondary">
          占位页,批 1a step 10 接入 react-hook-form + zod + useLogin。
        </p>
      </div>
    </div>
  )
}
