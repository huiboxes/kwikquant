{
  "name": "kwikquant-cli",
  "version": "0.1.0",
  "description": "KwikQuant 命令行工具——直连加密量化后端(行情 / 账户 / 组合 / 持仓)",
  "type": "module",
  "bin": {
    "kwikquant": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "gen:types": "openapi-typescript ${KWIKQUANT_API_DOCS:-http://localhost:8080/v3/api-docs} -o src/types/api-gen.ts",
    "gen:types:check": "pnpm gen:types && git diff --exit-code -- src/types/api-gen.ts"
  },
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "openapi-typescript": "^7.13.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
